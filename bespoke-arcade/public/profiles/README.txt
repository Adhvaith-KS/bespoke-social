Drop the three demo profile photos here, named exactly:

  adhvaith.jpg
  tarun.jpg
  shrey.jpg

The app looks for these paths (/profiles/<name>.jpg). Until a photo
exists, that person shows a gold initial coin instead. JPG expected —
if you only have PNGs, rename the extension in src/lib/demo-people.ts.
